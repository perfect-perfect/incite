<article class="post">
    <div class="single-post-container">
        <div class="title">
            <h3 class="related">Question</h3>
            <h3>{{title}}</h3>
            <div class = "post-text">
                <span style="white-space: pre-wrap">{{question}}</span>
            </div>
        </div>
        <div >
            <img src="{{user.avatar}}" alt="user avatar" class="single-post-avatar-container">
        </div>
        <div class="meta">
            {{!-- format_plural takes two arguments so we pass 'point' as the first and 'vote_count' as the second --}}
            {{vote_count}} {{format_plural "point" vote_count}} by {{user.username}} on
            {{format_date created_at}}
            |
            <a href="/post/{{id}}">{{answers.length}} {{format_plural "answer" answers.length}}</a>
        </div>
        <div>
            <button type="button" class="upvote-btn">upvote</button>
        </div>
    </div>
</article>
{{#if loggedIn}}
<script src="/javascript/upvote.js"></script>
{{/if}}